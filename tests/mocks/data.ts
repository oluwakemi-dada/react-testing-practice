// import { createProduct } from './db';
// import { faker } from '@faker-js/faker';

// // Seed faker for consistent results
// faker.seed(123);

// // Create and store products
// export const products = await Promise.all(
//   [1, 2, 3].map((id) =>
//     createProduct({
//       id,
//       name: faker.commerce.productName(),
//       price: faker.number.int({ min: 1, max: 100 }),
//     }),
//   ),
// );
